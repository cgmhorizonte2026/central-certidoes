const {Store}=require('../lib/store');const {credentials}=require('../lib/auth');const path=require('node:path');
let input='';process.stdin.setEncoding('utf8');process.stdin.on('data',c=>input+=c);process.stdin.on('end',()=>{let store;try{const data=JSON.parse(input.replace(/^\uFEFF/,''));store=new Store(process.env.DATA_DIR||path.join(__dirname,'..','data'));if(store.all('user').length)throw Error('Já existe administrador. Use a área Administração para criar usuários.');const u={...credentials(data.username,data.password),role:'admin',active:true};store.audit({type:'administrator_provisioned_locally'},u.username);store.put('user',u.username,u);console.log('Administrador criado. Entre na Central com as credenciais informadas.');}catch(e){console.error(e.message);process.exitCode=1;}finally{store?.close();}});
